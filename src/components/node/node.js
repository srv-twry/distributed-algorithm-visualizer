import React from 'react';
import "./node.css";

class Node extends React.Component {
    // Needs: id, uuid, x and y coordinates, isLeader.
    render() {
        return (
            <div id="node">
                
            </div>
        );
    }
}

export default Node;
